services:
  app:
    build: .
    image: finally-app
    container_name: finally-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/finally.db
    volumes:
      - ./db:/app/data
    restart: unless-stopped
